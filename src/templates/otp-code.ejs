<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ITSYBIZZ — One-Time Password</title>
    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Helvetica, Arial, sans-serif; background: #0b1020; margin: 0; padding: 24px; }
      .shell { max-width: 560px; margin: 0 auto; }
      .brand { display: flex; align-items: center; gap: 12px; color: #e5e7eb; margin-bottom: 14px; }
      .brand-logo { width: 40px; height: 40px; border-radius: 8px; background: #111827; object-fit: contain; border: 1px solid rgba(255,255,255,0.06); }
      .brand-name { font-size: 16px; letter-spacing: .4px; font-weight: 600; }
      .brand-sub { font-size: 12px; opacity: .75; }

      .card { background: linear-gradient(180deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02)); border: 1px solid rgba(255,255,255,0.12); border-radius: 16px; padding: 24px; box-shadow: 0 10px 30px rgba(0,0,0,.25); }
      .title { font-size: 20px; font-weight: 700; color: #f3f4f6; margin: 0 0 8px; letter-spacing: .3px; }
      .subtitle { color: #c7c9d1; margin: 0 0 16px; font-size: 14px; line-height: 1.5; }
      .code-wrap { background: #0f172a; border: 1px solid rgba(148,163,184,.25); border-radius: 12px; padding: 18px; text-align: center; margin: 14px 0 8px; }
      .code { font-size: 32px; letter-spacing: 8px; font-weight: 800; color: #e2e8f0; text-shadow: 0 2px 8px rgba(30, 64, 175, .35); }
      .hint { font-size: 12px; color: #94a3b8; margin-top: 4px; }

      .cta { margin-top: 18px; }
      .cta a { display: inline-block; background: #2563eb; color: #ffffff !important; text-decoration: none; padding: 10px 16px; border-radius: 10px; font-weight: 600; box-shadow: 0 8px 20px rgba(37,99,235,.35); }

      .footer { margin-top: 18px; font-size: 12px; color: #a1a1aa; }
      .divider { height: 1px; background: rgba(255,255,255,.08); margin: 18px 0; }
    </style>
  </head>
  <body>
    <div class="shell">
      <div class="brand">
        <img class="brand-logo" src="cid:app-logo" alt="ITSYBIZZ" />
        <div>
          <div class="brand-name">ITSYBIZZ</div>
          <div class="brand-sub">Powered by Deepnap Softech</div>
        </div>
      </div>

      <div class="card">
        <p class="title">Your verification code</p>
        <p class="subtitle">Use the code below to verify your email. This code is valid for 45 seconds.</p>
        <div class="code-wrap">
          <div class="code"><%= code %></div>
          <div class="hint">Do not share this code with anyone</div>
        </div>
        <div class="divider"></div>
        <div class="footer">If you didn’t request this, you can safely ignore this email.</div>
      </div>
    </div>
  </body>
  </html>


